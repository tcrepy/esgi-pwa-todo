{
  "name": "esgi-pwa-todo",
  "version": "1.0.0",
  "description": "",
  "main": "js/app.js",
  "scripts": {
    "build": "npm i && workbox generateSW workbox-config.js && json-server --watch db/db.json --port 8888",
    "serve": "polymer serve",
    "generate": "workbox generateSW workbox-config.js",
    "api": "json-server --watch db/db.json --port 8888"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.14.2",
    "polymer-cli": "^1.9.8",
    "serve": "^11.0.0",
    "workbox-build": "^4.3.1",
    "workbox-cli": "^4.3.1"
  },
  "dependencies": {
    "idb": "^4.0.3",
    "lit-element": "^2.1.0",
    "wcomponent-check-connexion": "^0.1.3"
  }
}
